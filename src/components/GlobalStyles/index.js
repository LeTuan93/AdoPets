import '../GlobalStyles.scss';
import { useState ,useEffect } from 'react';
import statusLogin from '../FormLogin/statusLogin';

function GlobalStyles({ children }) {
    return children;
}

export default GlobalStyles;
